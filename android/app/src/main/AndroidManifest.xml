<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Push bildirimleri (FCM). Android 13+ çalışma anında izin ister;
         tanılama satırı Bildirim tercihleri ekranının altındadır. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- REKLAM KİMLİĞİ KALDIRILDI (2026-08-19, Play ön kontrolü):
         Bu izinleri biz yazmadık; `firebase_analytics` kendi manifest'inde
         tanımlıyor ve manifest birleştirmesiyle uygulamaya geçiyordu. Play
         "Reklam Kimliği" beyanında "Hayır" dediğimiz hâlde izni pakette
         görüp çelişki bildirdi.
         Uygulama reklam GÖSTERMİYOR; Analytics reklam kimliği olmadan da
         çalışır (olay sayımı sürer, kişiselleştirilmiş reklam hedefleme
         zaten kullanılmıyor). İzni burada kaldırmak beyanı doğru tutar ve
         mağaza sayfasında gereksiz "reklam kimliği kullanır" görünmesini
         engeller. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove"/>
    <!-- KALDIRILAN İZİNLER (2026-08-10, Play Store hazırlığı):
         RECORD_AUDIO ve RECEIVE_BOOT_COMPLETED, Takip Merkezi modülüne
         aitti; modül 4476326'da silindi ve `record` paketi de pubspec'ten
         çıktı. Kullanılmayan izin Play'de veri güvenliği formunda
         açıklama gerektirir ve kurulum öncesi kullanıcıya gösterilir —
         mikrofon izni isteyen bir hizmet uygulaması güven kaybettirir. -->
    <!-- Kamera: profil/iş fotoğrafı (image_picker). required="false":
         kamerasız cihazlar da uygulamayı kurabilir (galeriden seçilir). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="İlanda Hizmet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: zamanlanmış bildirim + yeniden başlatma
             sonrası yeniden planlama alıcıları (Takip Merkezi hatırlatmaları). -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />


        <!-- FCM sistem bildirimleri: monokrom küçük ikon + marka rengi.
             (Renkli logo large icon istemci/FCM image ile; status bar tek renk.) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />
        <!-- Fotoğraf kırpma ekranı (image_cropper / uCrop).
             Kayıt edilmezse kırpıcı açılırken ActivityNotFoundException
             verir ve fotoğraf kırpılmadan yüklenir. -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher görünürlüğü (Android 11 / API 30+).
             ZORUNLU: API 30'dan itibaren paket görünürlüğü kısıtlıdır; bu blok
             olmadan `canLaunchUrl` false döner ve `launchUrl` tarayıcıyı
             AÇMAZ — üstelik hata da vermez, kullanıcı düğmenin çalışmadığını
             sanır. url_launcher eklentisi bu bloğu KENDİ manifest'inde
             taşımaz (yalnız örnek uygulamasında vardır), uygulama tanımlamak
             zorundadır.
             Kullanan yerler: menü altı ilandahizmet.com bağlantısı, profil
             sosyal medya bağlantıları, sohbetteki WhatsApp (wa.me) düğmesi,
             yasal metin bağlantıları. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
        <!-- tel: telefonu açığa çıkaran ustaya arama düğmesi. -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- mailto: destek / iletişim bağlantıları. -->
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
